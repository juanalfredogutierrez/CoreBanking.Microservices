apiVersion: v1
kind: Service

metadata:
  name: accountservice-service
  namespace: banking

spec:
  selector:
    app: accountservice

  ports:
  - port: 80
    targetPort: 5001
    nodePort: 30001

  type: NodePort